# InkScript 前端界面诊断报告

生成时间：2026-06-07  
检查工具：web-design-guidelines + diagnose  
检查范围：`novel2script/web/` 目录下的所有前端文件

---

## 一、执行摘要

本次诊断发现 **17 个问题**，按严重程度分类：
- 🔴 严重（影响功能/数据安全）：3 个
- 🟡 中等（影响用户体验）：9 个  
- 🟢 轻微（代码质量/可维护性）：5 个

---

## 二、严重问题（必须修复）

### 🔴 问题 1：缺少键盘导航支持（可访问性问题）

**位置**：`index.html` 第 203 行  
**代码**：
```html
<div @click="openProject(p.id)" class="project-card flex items-center justify-between" 
     tabindex="0" role="button" :aria-label="'打开项目 ' + p.name">
```

**问题**：
- 虽然添加了 `tabindex="0"` 和 `role="button"`，但**没有处理 Enter/Space 键**
- 键盘用户无法通过键盘激活这些"按钮"

**修复建议**：
```html
<div @click="openProject(p.id)" 
     @keydown.enter="openProject(p.id)"
     @keydown.space.prevent="openProject(p.id)"
     class="project-card flex items-center justify-between" 
     tabindex="0" role="button" :aria-label="'打开项目 ' + p.name">
```

**影响**：键盘用户和屏幕阅读器用户无法使用项目列表功能

---

### 🔴 问题 2：使用 `prompt()` 和 `alert()`（阻塞式对话框）

**位置**：`app.js` 第 316 行、345 行  
**代码**：
```javascript
const name = prompt('请输入项目名称：');
// ...
if (!confirm('确认删除该项目？项目将进入回收站。')) return;
```

**问题**：
- `prompt()` 和 `confirm()` 是**阻塞式**的，会冻结 UI
- 在 PyWebView 中可能表现不一致
- 无法自定义样式，用户体验差
- 屏幕阅读器支持不好

**修复建议**：
使用自定义的 Modal 对话框组件（已经在代码中使用了 `showShortcutsModal`，可以参考）

---

### 🔴 问题 3：没有错误处理和用户反馈（数据丢失风险）

**位置**：`app.js` 多处 API 调用  
**代码**：
```javascript
async loadProjects() {
    const res = await fetch(url);
    const data = await res.json();
    if (data.code === 0) {
        this.projects = data.data;
    }
    // 没有 else 分支处理错误！
}
```

**问题**：
- **没有 `try-catch`** 包裹 API 调用
- 如果网络失败或后端报错，用户**看不到任何错误提示**
- 可能导致数据不一致或丢失

**修复建议**：
```javascript
async loadProjects() {
    try {
        const res = await fetch(url);
        if (!res.ok) throw new Error(`HTTP ${res.status}`);
        const data = await res.json();
        if (data.code === 0) {
            this.projects = data.data;
        } else {
            throw new Error(data.message || '加载项目失败');
        }
    } catch (error) {
        this.showToast(`错误：${error.message}`, 'error');
        console.error('[loadProjects]', error);
    }
}
```

**影响**：网络异常时用户不知道发生了什么，可能误操作导致数据丢失

---

## 三、中等问题（建议修复）

### 🟡 问题 4：CDN 依赖过多（加载性能问题）

**位置**：`index.html` 第 7-36 行  
**代码**：
```html
<script src="https://cdn.tailwindcss.com"></script>
<script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.7/dist/chart.umd.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/sortablejs@1.15.0/Sortable.min.js"></script>
<!-- 还有 CodeMirror 的 10+ 个 ES 模块 -->
```

**问题**：
- **12 个外部 CDN 依赖**，增加页面加载时间
- 如果 CDN 不可用（如内网环境），应用完全无法使用
- 没有 `integrity` 属性，存在安全风险

**修复建议**：
1. 将核心依赖打包到本地（使用 Vite/webpack）
2. 至少添加 `integrity` 和 `crossorigin` 属性：
```html
<script src="https://cdn.tailwindcss.com"
        integrity="sha384-..."
        crossorigin="anonymous"></script>
```

**影响**：首次加载慢，内网环境无法使用

---

### 🟡 问题 5：缺少 Loading 状态管理（用户体验问题）

**位置**：`app.js` 多个异步方法  
**观察**：
- `loadProjects()`、`loadNovel()`、`saveScript()` 等方法没有 Loading 状态
- 用户点击按钮后不知道是否在处理

**修复建议**：
为每个异步操作添加 Loading 状态：
```javascript
async loadProjects() {
    this.loadingProjects = true;  // 新增状态
    try {
        const res = await fetch(url);
        // ...
    } finally {
        this.loadingProjects = false;
    }
}
```
在按钮上显示 Loading：
```html
<button @click="loadProjects()" :disabled="loadingProjects">
    <span x-show="loadingProjects">加载中...</span>
    <span x-show="!loadingProjects">刷新</span>
</button>
```

---

### 🟡 问题 6：编辑器自动保存可能丢失数据

**位置**：`app.js`（自动保存计时器）  
**推测代码**：
```javascript
this._novelSaveTimer = setTimeout(() => {
    this.saveNovel();
}, 2000);
```

**问题**：
- 如果用户在 2 秒内关闭窗口，可能丢失数据
- 没有"正在保存"的提示
- 保存失败时没有重试机制

**修复建议**：
1. 在 `beforeunload` 事件中检查是否有未保存的更改
2. 添加保存状态指示（"已保存" / "保存中" / "保存失败"）
3. 保存失败时自动重试（最多 3 次）

---

### 🟡 问题 7：没有表单验证（数据质量问题）

**位置**：项目创建/编辑功能  
**问题**：
- `prompt()` 没有验证项目名称是否为空或包含特殊字符
- 设置页面的 API Key、URL 等没有格式验证

**修复建议**：
使用自定义 Modal 时添加验证：
```javascript
createProject() {
    if (!this.newProjectName || this.newProjectName.trim().length === 0) {
        this.showToast('项目名称不能为空', 'error');
        return;
    }
    if (this.newProjectName.length > 50) {
        this.showToast('项目名称不能超过 50 个字符', 'error');
        return;
    }
    // ...
}
```

---

### 🟡 问题 8：情绪曲线图表可能没有销毁（内存泄漏）

**位置**：`app.js` 第 76 行  
**代码**：
```javascript
emotionCurveChart: null,  // Chart.js 实例
```

**问题**：
- 如果多次打开/关闭情绪曲线面板，旧图表实例可能没有销毁
- Chart.js 需要手动调用 `destroy()` 释放内存

**修复建议**：
```javascript
toggleEmotionCurve() {
    this.showEmotionCurve = !this.showEmotionCurve;
    if (!this.showEmotionCurve && this.emotionCurveChart) {
        this.emotionCurveChart.destroy();
        this.emotionCurveChart = null;
    }
}
```

---

### 🟡 问题 9：拖拽分隔条可能没有移除事件监听（内存泄漏）

**位置**：`app.js`（编辑器分栏拖拽功能）  
**推测代码**：
```javascript
_startDrag() {
    document.addEventListener('mousemove', this._onDrag);
    document.addEventListener('mouseup', this._stopDrag);
}
```

**问题**：
- 如果没有正确移除 `mousemove` 和 `mouseup` 监听，会导致内存泄漏
- 可能在拖拽结束后仍然触发事件处理

**修复建议**：
确保在 `mouseup` 时移除监听：
```javascript
_stopDrag() {
    document.removeEventListener('mousemove', this._onDrag);
    document.removeEventListener('mouseup', this._stopDrag);
    this._isDragging = false;
}
```

---

### 🟡 问题 10：没有快捷键冲突处理

**位置**：`app.js` 第 190-223 行（快捷键系统）  
**问题**：
- 如果用户在输入框中按 `Ctrl+S`，会触发保存，但也可能触发浏览器的保存对话框
- `?` 快捷键可能在中文输入法下误触发

**修复建议**：
```javascript
document.addEventListener('keydown', (e) => {
    const tag = e.target.tagName.toLowerCase();
    const isInput = tag === 'input' || tag === 'textarea' || tag === 'select';
    
    // 在输入框中不触发快捷键（除了 Escape）
    if (isInput && e.key !== 'Escape') return;
    
    // ...
});
```

---

### 🟡 问题 11：SSE 连接可能没有正确关闭（资源泄漏）

**位置**：`app.js`（转换进度 SSE 连接）  
**代码**：
```javascript
eventSource: null,  // SSE 连接
```

**问题**：
- 如果用户在转换过程中关闭编辑器或切换到其他页面，SSE 连接可能没有关闭
- 会导致后端继续推送数据，浪费资源

**修复建议**：
```javascript
// 在 Alpine.js 的 `destroy` 生命周期或切换页面时关闭
cleanup() {
    if (this.eventSource) {
        this.eventSource.close();
        this.eventSource = null;
    }
}
```

---

### 🟡 问题 12：没有离线支持（PWA 功能缺失）

**观察**：
- 没有 Service Worker
- 没有离线缓存策略
- 没有 `manifest.json`

**影响**：
- 桌面应用在网络不稳定时无法使用
- 无法作为 PWA 安装到系统

**修复建议**：
添加 Service Worker 和基础离线支持（可以参考 Workbox）

---

## 四、轻微问题（代码质量改进）

### 🟢 问题 13：CSS 变量没有 fallback

**位置**：`app.css` 第 4-22 行  
**代码**：
```css
:root {
    --color-primary: #d97757;
}
body {
    color: var(--color-text);  /* 如果 --color-text 未定义会怎样？ */
}
```

**修复建议**：
添加 fallback 值：
```css
body {
    color: var(--color-text, #141413);
}
```

---

### 🟢 问题 14：动画可能没有尊重 `prefers-reduced-motion`

**位置**：`app.css` 第 202-282 行（所有 `@keyframes`）  
**问题**：
- 有 6 个动画效果
- 但没有为偏好减少动画的用户提供替代方案

**修复建议**：
```css
@media (prefers-reduced-motion: reduce) {
    *, *::before, *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
    }
}
```

---

### 🟢 问题 15：Tailwind CSS 通过 CDN 引入（性能问题）

**位置**：`index.html` 第 7 行  
**代码**：
```html
<script src="https://cdn.tailwindcss.com"></script>
```

**问题**：
- Tailwind CDN 是 **运行时生成 CSS**，性能差
- 生产环境不应该使用 CDN 版本

**修复建议**：
使用构建工具（Vite/webpack）预编译 Tailwind

---

### 🟢 问题 16：没有使用语义化 HTML

**观察**：
- 大量使用 `<div>` 和 `<button>`
- 没有使用 `<nav>`、`<main>`、`<section>`、`<article>` 等语义化标签

**影响**：
- 屏幕阅读器用户难以理解页面结构
- SEO 不友好（虽然这是桌面应用，但仍然是好习惯）

**修复建议**：
```html
<!-- 现在 -->
<div class="feature-card cursor-pointer">

<!-- 改进后 -->
<article class="feature-card" tabindex="0" role="link">
```

---

### 🟢 问题 17：代码中没有 JSDoc 注释

**位置**：`app.js`（所有方法）  
**问题**：
- 没有类型注解
- 没有参数说明
- 没有返回值说明

**影响**：
- 维护困难
- 新成员上手慢
- 无法使用 IDE 的智能提示

**修复建议**：
添加 JSDoc 注释：
```javascript
/**
 * 加载项目列表
 * @param {string} search - 搜索关键词
 * @param {string} sortBy - 排序字段
 * @param {string} order - 排序方向
 * @returns {Promise<void>}
 */
async loadProjects() { ... }
```

---

## 五、诊断总结与优先级建议

### 立即修复（本周内）
1. 🔴 问题 1：添加键盘导航支持
2. 🔴 问题 3：添加全局错误处理
3. 🟡 问题 5：添加 Loading 状态

### 短期修复（2 周内）
4. 🔴 问题 2：替换 `prompt()`/`alert()` 为自定义 Modal
5. 🟡 问题 6：改进自动保存逻辑
6. 🟡 问题 7：添加表单验证

### 长期改进（1 个月内）
7. 🟡 问题 4：优化 CDN 依赖
8. 🟡 问题 8-9：修复内存泄漏
9. 🟢 问题 14：添加 `prefers-reduced-motion` 支持

---

## 六、测试方法

### 手动测试清单
- [ ] 使用 Tab 键导航整个应用，确保所有交互元素都可聚焦
- [ ] 使用 Enter/Space 键激活所有按钮和链接
- [ ] 断开网络，测试错误处理和用户反馈
- [ ] 快速连续点击按钮，测试 Loading 状态是否正确
- [ ] 在输入框中编辑，等待自动保存，然后立即关闭窗口，检查数据是否丢失

### 自动化测试建议
- 使用 Playwright 编写 E2E 测试
- 使用 axe DevTools 进行可访问性测试
- 使用 Lighthouse 进行性能测试

---

## 七、参考资源

- **Web Interface Guidelines**：https://raw.githubusercontent.com/vercel-labs/web-interface-guidelines/main/command.md
- **WCAG 2.1 可访问性指南**：https://www.w3.org/WAI/WCAG21/quickref/
- **Chart.js 内存管理**：https://www.chartjs.org/docs/latest/developers/api.html#destroy
- **Alpine.js 最佳实践**：https://alpinejs.dev/advanced/state

---

**报告结束**

**下一步**：
1. 和团队评审这份报告
2. 按优先级制定修复计划
3. 分配责任和截止日期
4. 修复后重新运行诊断
