# InkScript 启动指南

> 快速启动和使用指南

---

## 🚀 快速启动

### 安装依赖

```bash
cd d:\bianchenglianxi\project\InkScript
pip install -e ".[dev]"
```

这会安装所有依赖，包括：
- FastAPI（Web 框架）
- PyWebView（桌面窗口）
- Pydantic（数据验证）
- 其他依赖...

### 启动方式1：Web 服务（推荐）

```bash
novel2script serve
```

然后打开浏览器访问：`http://127.0.0.1:8000`

**优点**：
- 简单易用
- 不需要桌面窗口
- 可以在任何浏览器中打开

### 启动方式2：桌面应用

```bash
novel2script gui
```

这会启动一个独立的桌面窗口。

**注意**：
- 需要安装 `pywebview`（已经帮你安装好了）
- 第一次启动可能会慢一点

### 启动方式3：CLI 直接转换

```bash
novel2script convert input.txt output.yaml --model gpt-4
```

直接在命令行中转换小说为剧本，不需要界面。

---

## 📖 使用流程

### 1. 创建项目

1. 打开应用（Web 或桌面）
2. 点击"新建项目"
3. 输入项目名称

### 2. 编辑小说

1. 在项目列表中点击项目
2. 进入编辑器
3. 在左侧面板粘贴或输入小说原文
4. 点击"保存小说"

### 3. 转换剧本

1. 确保已经保存小说
2. 点击"开始转换"
3. 等待进度条完成
4. 右侧面板会显示转换后的剧本 YAML

### 4. 编辑剧本

1. 在右侧面板直接编辑 YAML
2. 点击"保存剧本"

### 5. 配置 API

1. 点击顶部"设置"
2. 输入 API Base URL（如 `https://api.openai.com/v1`）
3. 输入 API Key
4. 选择模型名称
5. 点击"保存设置"

---

## 🐛 常见问题

### 问题1：无法启动服务

**解决方案**：
```bash
# 检查端口是否被占用
netstat -ano | findstr :8000

# 如果被占用，换个端口
novel2script serve --port 8001
```

### 问题2：桌面窗口无法启动

**解决方案**：
```bash
# 确保 pywebview 已安装
pip install pywebview

# 如果还是不行，用 Web 模式
novel2script serve
```

### 问题3：转换失败

**可能原因**：
- API Key 未配置或不正确
- 网络连接问题
- 模型名称错误

**解决方案**：
1. 检查设置中的 API 配置
2. 查看浏览器控制台或终端输出
3. 尝试用 CLI 模式测试

---

## 📁 项目文件位置

- **项目数据**：`~/.novel2script/projects/` （每个项目一个目录）
- **配置文件**：`~/.novel2script/config.json`
- **日志**：终端输出

---

## 🔧 开发模式

如果你想修改代码并实时查看效果：

```bash
# Web 模式（自动重载）
novel2script serve --reload

# 修改前端文件后，刷新浏览器即可
```

---

## 📚 更多信息

- 查看 `README.md` 了解项目介绍
- 查看 `docs/` 目录了解详细设计
- 查看 `PROGRESS.md` 了解当前进度

---

**最后更新**：2026-06-05
